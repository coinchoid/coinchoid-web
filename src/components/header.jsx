import React from 'react';
import {render} from 'react-dom';
import { AppBar } from 'react-toolbox';


class Header extends React.Component {
  render () {
    return (
      <AppBar>Coinchoïd</AppBar>
    )
  }
}

export default Header;
